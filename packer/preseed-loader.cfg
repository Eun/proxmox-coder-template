# Minimal preseed — mounts second CD and chainloads the full preseed
d-i preseed/early_command string mkdir -p /tmp/preseed && mount /dev/sr1 /tmp/preseed && cp /tmp/preseed/preseed.cfg /tmp/full-preseed.cfg && umount /tmp/preseed
d-i preseed/include string /tmp/full-preseed.cfg