# Minimal preseed — mounts second CD and chainloads the full preseed
d-i preseed/early_command string mkdir -p /tmp/preseed && mount /dev/sr1 /tmp/preseed && debconf-set-selections /tmp/preseed/preseed.cfg